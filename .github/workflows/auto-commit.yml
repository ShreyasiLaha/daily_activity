name: Daily Auto Commit

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make a change
        run: echo "$(date)" >> activity.txt

      - name: Commit changes
        run: |
          git config --local user.name "ShreyasiLaha"
          git config --local user.email "ShreyasiLaha@users.noreply.github.com"
          git add -A
          git commit -m "Daily update" || echo "No changes"

      - name: Push changes
        run: git push origin main
